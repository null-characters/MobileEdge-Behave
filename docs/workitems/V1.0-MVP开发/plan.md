# V1.0 MVP 开发规划

## 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V1.0 | 2026-07-21 | 初稿 |
| V1.1 | 2026-07-21 | 根据 tasks.md V1.1 变更同步更新开发阶段划分 |

---

## 一、背景与目标

### 1.1 背景

利用旧安卓手机（红米 K60）作为边缘计算节点，搭建本地图像识别系统，用于办公室环境下的员工在岗/离岗状态监测。所有数据处理在本地完成，保护用户隐私。

### 1.2 目标

开发 V1.0 MVP 版本，实现核心功能：

- 人形检测（俯视视角，固定全画面）
- 在岗/离岗状态判断（状态机，连续 5 帧确认）
- 状态变化日志记录（Room 数据库）
- 简单统计界面（当日数据）
- CSV 数据导出
- MIUI 后台持续运行（Foreground Service）

### 1.3 非目标

V1.0 不包含以下功能（留待后续版本）：

- ROI 配置
- 实时告警/推送
- Web 仪表盘
- 多人支持
- 工作/摸鱼行为分类

---

## 二、当前基线

### 2.1 已有资产

| 资产 | 状态 | 说明 |
|------|------|------|
| 设计文档 | 已完成 | `design/behavior-monitor-design.md` |
| 架构文档 | 已完成 | `docs/standards/skills/architecture.md` |
| 设备信息 | 已完成 | 红米 K60 已清理，可用作开发测试设备 |
| Android 项目 | 未创建 | 需从零搭建 |

### 2.2 硬件约束

| 项目 | 规格 | 影响 |
|------|------|------|
| 设备 | 红米 K60 | 目标设备 |
| 处理器 | 骁龙 8+ Gen 1 | NNAPI 加速支持良好 |
| RAM | 12GB | 充足 |
| 存储 | 256GB | 充足（可用 213GB） |
| 系统 | MIUI V816 (Android 15) | 后台保活需专项处理 |

### 2.3 技术债务

- 无（新项目）

---

## 三、方案设计

### 3.1 架构设计

采用 **MVVM + Clean Architecture** 三层架构：

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Compose UI  │  │  ViewModel  │  │  UiState    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
├─────────────────────────────────────────────────────────────┤
│                     Domain Layer                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ UseCase     │  │ StateMachine│  │ Repository  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
├─────────────────────────────────────────────────────────────┤
│                      Data Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │    Room     │  │  TFLite     │  │  CameraX    │         │
│  │  (SQLite)   │  │  (模型推理)  │  │ (视频采集)  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心数据流

```
CameraX (5fps)
    ↓
ImageAnalyzer.analyze(imageProxy)
    ↓
TFLite 推理 (EfficientDet-Lite0)
    ↓
DetectionResult (bbox, confidence)
    ↓
StateMachine.updateState()
    ↓
StateEvent (状态变化)
    ↓
Room 数据库存储
    ↓
ViewModel 聚合统计
    ↓
Compose UI 展示 / CSV 导出
```

### 3.3 状态机设计

```
状态: UNKNOWN | PRESENT | ABSENT

参数（俯视场景优化）：
- 置信度阈值: 0.5（俯视场景检测框较小，置信度偏低）
- 连续帧确认: 5帧（约1秒，防止瞬时误检）
- 检测框面积阈值: > 5% 画面（排除远处路人干扰）

转换规则：
  UNKNOWN ──[检测到人×5帧]──→ PRESENT
  UNKNOWN ──[未检测到人×5帧]──→ ABSENT
  PRESENT ──[未检测到人×5帧]──→ ABSENT（记录离岗时间点）
  ABSENT ──[检测到人×5帧]──→ PRESENT（记录返回时间点）
```

### 3.4 数据模型

```kotlin
// 状态变化事件
@Entity(tableName = "state_events")
data class StateEvent(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val timestamp: Long,        // 时间戳（毫秒）
    val fromState: String,      // PRESENT/ABSENT/UNKNOWN
    val toState: String,        // ABSENT/PRESENT
    val confidence: Float       // 检测置信度
)

// 每日统计（运行时计算，不持久化）
data class DailySummary(
    val date: String,                   // YYYY-MM-DD
    val totalPresentMs: Long,           // 在岗总时长（毫秒）
    val totalAbsentMs: Long,            // 离岗总时长（毫秒）
    val absenceEvents: List<AbsenceRecord>  // 离岗记录列表
)

data class AbsenceRecord(
    val startTime: Long,    // 离岗开始时间
    val endTime: Long,      // 返回时间
    val durationMs: Long    // 离岗时长
)
```

### 3.5 MIUI 后台保活策略

**必须实现**：

| 措施 | 说明 |
|------|------|
| Foreground Service | 常驻通知栏，显示"监测中"状态 |
| WakeLock | 屏幕关闭时保持 CPU 运行 |
| 服务守护 | 异常退出后自动重启 |

**引导用户操作**：

| 操作 | 说明 |
|------|------|
| 关闭电池优化 | 系统设置 → 电池 → 应用电池优化 |
| 开启自启动权限 | 系统设置 → 应用 → 自启动管理 |
| 设置"无限制"后台运行 | 系统设置 → 应用 → 后台运行 |
| 锁定最近任务 | 最近任务界面下拉锁定 |

### 3.6 技术选型

| 模块 | 技术方案 | 版本要求 |
|------|----------|----------|
| 开发语言 | Kotlin | 1.9+ |
| UI 框架 | Jetpack Compose | 1.5+ |
| 摄像头 | CameraX | 1.3+ |
| 模型推理 | TensorFlow Lite | 2.14+ |
| 数据存储 | Room | 2.6+ |
| 后台服务 | Foreground Service | - |

### 3.7 AI 模型

| 功能 | 模型 | 大小 | 延迟 |
|------|------|------|------|
| 人形检测 | EfficientDet-Lite0 | 4.4MB | ~30ms |
| 人形检测 (备选) | MobileNet SSD v2 | 6.7MB | ~25ms |

---

## 四、风险点与避坑说明

### 4.1 技术风险

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| MIUI 后台被杀死 | 高 | 高 | Foreground Service + 引导用户加白名单 |
| 模型精度不足 | 中 | 高 | 选择成熟模型，后期可训练定制模型 |
| 过热降频 | 中 | 中 | 智能采样 + 散热措施 |
| 俯视场景检测率低 | 中 | 中 | 调低置信度阈值（0.5），测试验证 |

### 4.2 已知避坑点

1. **MIUI 后台保活**：MIUI 后台管理极其激进，必须：
   - 使用 Foreground Service + 常驻通知
   - 引导用户关闭电池优化、开启自启动
   - 测试屏幕关闭后的持续运行能力

2. **CameraX 生命周期**：后台服务中需正确绑定生命周期，避免内存泄漏

3. **TFLite NNAPI 加速**：部分设备 NNAPI 兼容性问题，需准备 GPU/CPU 回退方案

4. **俯视场景检测**：
   - 检测框可能较小，置信度偏低
   - 已将阈值从 0.7 调整为 0.5
   - 需实际测试验证准确率

5. **隐私合规**：
   - 不存储原始图像
   - 明确告知被监控者
   - 数据仅用于生产力分析

---

## 五、验收标准

### 5.1 功能验收

| 编号 | 验收项 | 验收条件 |
|------|--------|----------|
| F-01 | 后台持续运行 | App 连续运行 8 小时不退出（MIUI 保活） |
| F-02 | 人形检测准确率 | 俯视场景检测准确率 > 90% |
| F-03 | 状态变化延迟 | 从检测到人到状态变化 < 3 秒（连续 5 帧确认） |
| F-04 | 日志记录 | 状态变化事件正确记录时间点、前后状态、置信度 |
| F-05 | 统计界面 | 显示当日数据：在岗时长、离岗时长、离岗次数 |
| F-06 | CSV 导出 | 导出格式正确，包含 timestamp、from_state、to_state、confidence |
| F-07 | 屏幕关闭运行 | 屏幕关闭后服务继续运行 |

### 5.2 性能验收

| 编号 | 验收项 | 验收条件 |
|------|--------|----------|
| P-01 | 模型推理延迟 | < 30ms（NNAPI 加速） |
| P-02 | 功耗 | < 5W（插电运行温度正常） |
| P-03 | 内存占用 | < 300MB |
| P-04 | 设备温度 | < 45°C |

### 5.3 稳定性验收

| 编号 | 验收项 | 验收条件 |
|------|--------|----------|
| S-01 | 长期运行 | 连续运行 8 小时无崩溃 |
| S-02 | 异常恢复 | 异常退出后自动重启 |

### 5.4 隐私验收

| 编号 | 验收项 | 验收条件 |
|------|--------|----------|
| R-01 | 本地处理 | 所有图像处理在本地完成，无网络请求 |
| R-02 | 数据最小化 | 不存储原始图像，只存储行为日志 |

---

## 六、开发阶段划分

### 阶段一：项目搭建（T-01, T-02）

- 创建 Android 项目，配置基础依赖
- 搭建 MVVM 架构骨架

### 阶段二：核心功能（T-03 ~ T-09）

- CameraX 视频帧采集（固定 5fps）
- TFLite 模型集成与 NNAPI 推理
- 人形检测后处理
- 状态机实现
- Room 数据存储
- ViewModel 状态管理

### 阶段三：保活完成（T-10）

- Foreground Service + WakeLock
- 异常自动重启
- 屏幕关闭后持续运行

### 阶段四：UI 完成（T-11 ~ T-14）

- 统计界面（Compose）
- CSV 导出功能
- MIUI 保活引导界面
- 权限请求流程

### 阶段五：验收通过（T-15）

- 性能优化
- 功能测试
- 稳定性测试

---

## 七、依赖与前置条件

### 7.1 外部依赖

| 依赖 | 说明 | 获取方式 |
|------|------|----------|
| EfficientDet-Lite0 模型 | 人形检测模型 | TensorFlow Hub 下载 |
| Android Studio | 开发环境 | 已安装 |
| ADB | 设备调试 | Android SDK |

### 7.2 前置条件

- [x] 红米 K60 已清理，可用作开发测试设备
- [x] ADB 调试已启用
- [x] 设计文档已完成
- [x] 架构文档已完成

---

## 八、后续版本规划

| 版本 | 功能 | 说明 |
|------|------|------|
| V1.5 | 在岗时长统计 + 离岗告警 | 基于 V1.0 数据 |
| V2.0 | 工作/摸鱼行为分类 | 需姿态估计模型 |
| V3.0 | Web 仪表盘 + 多行为识别 | HTTP Server |
